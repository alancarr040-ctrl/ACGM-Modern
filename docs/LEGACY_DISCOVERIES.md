Legacy Discoveries

Tree Icons

Normal -> guy.bmp

Monarch -> M.bmp

Mule -> muleico.jpg

PK -> pkico.jpg

Rescue -> rescueico.jpg

Character Save

VB6 preserves trailing empty fields.

Awards are serialized in final field.

Tree Parser

Tree.dat uses legacy compact record format.

Protocol

MSG_UPDATE_CHAR_INFO preserves 33 fields.

Do not collapse empty trailing values.

Search System
  The VB6 client implemented Search as a dedicated workspace tab, not as a modal dialog.
  
  The Search workspace included:

	- Rank filter
	- Race filter
	- Skill filter grid
	- Search
	- Reset
	- Results list

  Modern enhancements such as live filtering, sorting, and export are intentionally deferred until Phase 0.12.